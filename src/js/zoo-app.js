(function() {
  'use strict';

  function Animal(name, dob) {
    this.name = name || 'Random';
    this.dob = dob;
  }

// Animal();
console.log(new Animal('sandy', 10));
  // function Polarbear(name, color, birth) {
  //   console.log(this, arguments);
  // }
  //
  // var sandy = new Polarbear ('sandy', 'white', true,)
}());
